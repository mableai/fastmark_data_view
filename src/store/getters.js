const getters = {
  playMethod: state => state.data.playMethod,
  activityId: state => state.data.activityId,
  activityStartDate: state => state.data.activityStartDate,
  businessList: state => state.data.businessList,
}
export default getters
